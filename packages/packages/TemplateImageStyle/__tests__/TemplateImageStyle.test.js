'use strict';

const TemplateImageStyle = require('..');
const assert = require('assert').strict;

assert.strictEqual(TemplateImageStyle(), 'Hello from TemplateImageStyle');
console.info('TemplateImageStyle tests passed');
